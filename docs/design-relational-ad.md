# Superseded — merged into `design.md`

This document's content (the v2 query-level reverse-mode AD design: the
Substrait pivot, the marker-function mechanism, the five transpose rules, the
backward-program emitter, and the spikes that verify each) has been merged
into [`design.md`](design.md), primarily **§4 — v2: query-level reverse-mode
AD**, with the supporting evidence and rejected alternatives moved to the
**Decision Log**'s `S1`–`S5` entries at the end of that document.

This file is kept only so old links don't 404. Read `design.md` — it's now the
single, self-contained design document, covering v1 and v2 as one continuous
story.
